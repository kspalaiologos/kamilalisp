
#ifndef _PRIME_LIB_HPP
#define _PRIME_LIB_HPP

#include "../atom.hpp"
#include "../env.hpp"
#include "../list.hpp"

namespace primelib {

define_callable(prime);
define_callable(p_factors);
define_callable(p_ufactors);
define_callable(divisors);
define_callable(mobius_mu);
define_callable(p_until);

}

#endif
