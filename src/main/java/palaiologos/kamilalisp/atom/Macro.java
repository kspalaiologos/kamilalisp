package palaiologos.kamilalisp.atom;

/**
 * A native mapping function from a list to a list that transforms code.
 * The arguments are not evaluated. Expanded during evaluation. Every time.
 */
public interface Macro extends Callable {
}
